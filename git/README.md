# Git Configuration

> ⚠️ **OPTIONAL OVERRIDE**
>
> `ansible-collection-setup` is the PRIMARY source for Git configuration.
> These files are only for quick personal tweaks.
>
> Prefer updating the ansible templates at:
> - `ansible-collection-setup/roles/git/templates/gitconfig.j2`
> - `ansible-collection-setup/roles/git/templates/gitignore_global.j2`
